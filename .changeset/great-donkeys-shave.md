---
"prettier-plugin-marko": minor
---

Report Marko syntax errors instead of silently dropping content. The parser stops at the first error, so anything after it was previously missing from the output.
